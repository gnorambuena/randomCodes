#include <iostream>
#include <vector>
using namespace std;
int main(){
	cin.tie(0);ios::sync_with_stdio(0);
	vector<int>v (6,0);
	//cout<<v[-1]<<"\n";
	cout<<v[v.size()+6] = 10000;
	//for(int i = 0 ; i <= v.size() ; i++)cout<<v[i]<<"\n";
	return 0;
}